# Web3Forms access key — get a free key at https://web3forms.com
# Sent from the BROWSER (Web3Forms' free plan only allows client-side calls),
# so it must be NEXT_PUBLIC. The access key is public by design; restrict abuse
# by setting "Allowed Domains" in your Web3Forms dashboard.
NEXT_PUBLIC_WEB3FORMS_KEY=your-web3forms-access-key

# Postgres (optional) — persist inquiries to a DB.
# On Vercel: Storage → Marketplace → Neon (Postgres); the integration injects
# POSTGRES_URL or DATABASE_URL automatically (the app accepts either).
# Locally: paste a Neon / Vercel Postgres connection string below.
# When set, each inquiry is saved to an "inquiries" table (auto-created).
POSTGRES_URL=
